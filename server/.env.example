# Copy this to .env and fill in your secret before running docker compose up.
# Use a long random string — this is the shared key between the server
# and the app clients. Keep it out of version control.
BORE_SECRET=replace-with-a-long-random-secret
